# To Do: Get a minimal configuration for running the program
image: ubuntu:14.04

before_script:
  - apt-get update
  - apt-get install -y pkg-config
  - apt-get install -y libpng-dev  libfreetype6-dev
  - apt-get install -y python python3 git
  - apt-get install -y python-pip python3-pip
  - pip install -U pytest
  - pip install -U pyyaml
  #- pip install numpy matplotlib
  - apt-get install -y python-numpy python-matplotlib
# This file is generated by GitLab CI
test:
  script:
  # just update all submodules if present
  - git submodule update --init
  # install package using setup.py
  #- python setup.py install
  # run unit tests
  - python -m pytest tests/
  tags:
  - docker
  except:
  - tags
